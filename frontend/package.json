{
  "name": "tulipsedu-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.22.0",
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.1",
    "@types/qrcode-generator": "^0.0.16",
    "typescript": "^5.5.0",
    "vite": "^5.3.0"
  }
}
